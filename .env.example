# 环境变量示例文件 —— 复制本文件为 .env,填入你自己的密钥。
# .env 已被 .gitignore 忽略,不会提交到版本库;本文件只写变量名、不写真实值。

# TMDB 密钥:填 "API Key (v3 auth)"(32 位字符)或
# "API Read Access Token (v4)"(eyJ... 开头的一长串 token)都可以,程序会自动识别。
# 申请方式见 README「接入 TMDB(v1.3)」一节。
TMDB_API_KEY=
